# Research index

One line per finding, newest first. Each line carries the subject the file settles
and enough of the finding to decide whether to open it, ending in the filename —
the same contract `LOG/index.md` states for itself, and for the same reason: a
session should be able to tell what a file answers without opening it.

- **Write failures per item before and after the state server's tools (2026-09-12)** — the same sixteen planning sessions as the token-cost audit plus seven build runs, five classes counted from the transcripts: errored tool results per item 0.09 before and 0.09 after, queue-tool refusals one in each window, repeats one and two; lint flags fell 1.73 to 0.75 per item but that class measures how long a flag stood times shell commands run, not writes gone wrong; the decision log keeps 500 lines and reached back only to 2026-09-11, so refusals could not be counted for either window. Settles that no attributable difference exists and the MCP post's draft stands. Carries a frame assessment → `mcp-write-failures-before-after.md`
- **Token cost of planning sessions before and after the state server's queue tools (2026-09-11)** — eight sessions each side of 2026-09-06 read from the transcripts' usage blocks: 7.15M tokens per item processed before, 6.29M after (12%), with per-session spread of 4.2M to 19.1M swamping it; 97% of every total is cache reads of the conversation; tool calls per item did not fall (8.6 to 9.3); only half the after-window queue writes went through the server. Settles that no token saving can be claimed to the tester; the server's gain is refusal at the door. Carries a frame assessment and a closing paragraph for a reader outside the project → `mcp-token-cost-before-after.md`
- **What a spec is expected to contain, and whether this project's SPEC is the document the tools expect (2026-09-06)** — Anthropic's own SPEC.md recipe is a per-feature brief (files, interfaces, out-of-scope, a verification step) executed by one fresh session; Spec Kit's spec is per feature with prioritised testable stories, MUST requirements and measurable success criteria, the constitution kept separate; the field's answer to agents not following a spec is a per-task slice plus a mechanical drift gate, never a whole-document read. So the document those sources call a spec is this method's work item, and nothing prescribes a build reading a 98KB product document whole; what SPEC does uniquely is the product-truth role planning consults. Does not settle whether dropping the run-start read costs anything. Carries a frame assessment → `spec-what-tools-expect-2026-09.md`
- **Is SPEC used as a source of truth or maintained dead weight — sixteen Hexboard and Taskflowapp transcripts scored (2026-09-06)** — every planning session in the sample decided something on a SPEC sentence and seven of eight also edited SPEC; builds split three decided-or-filed, one referred, four opened it at run start and never touched it again. Thinking blocks named SPEC twice in sixteen sessions, so replies are the reasoning channel. Also shows SPEC carrying long-term direction that gated queue items, and one SPEC sentence whose met condition stood two days. Carries a frame assessment → `spec-usage-versus-maintenance.md`
- **Claude Code's security guidance, read 2026-09-03 — no risk list to comply with; prompt injection is the one named threat, and `/security-review` is the affordable weekly pass** — the guidance page describes the permission model, built-in protections, prompt-injection safeguards and two best-practice lists; the scanner plugin page describes a multi-agent scan needing a paid plan and `python3` on the PATH. Settles that the red-flag scope lacks prompt injection through observed content, and that the maintenance sweep's security pass uses `/security-review`. Re-read each sweep turn. Carries a frame assessment → `claude-code-security-guidance.md`

- **Self-hosting onboarding (standing line, opened 2026-08-31).** The accretion point for what a person — porter, consumer, or between — needs to become self-hosting on Throughliner; first data point is the OpenCode assessment pack, with the Egnatia-OC injection as the live experiment. Add findings here, work stays in the queue → self-hosting-onboarding.md
- **Claude Code MCP registration (2026-08-31).** A project attaches a local stdio server via `.mcp.json` at the project root, supported in the desktop app, gated by a one-time trust dialog; a plugin can bundle one via `plugin.json`'s `mcpServers`, so dogfood-here-then-promote needs no change to the server itself. Read from official docs, untested → claude-code-mcp-registration.md

- **MCP support in the port harnesses (2026-08-31).** Both live ports' harnesses — OpenCode and Kilo Code — run local stdio MCP servers natively through their own config files, so a Throughliner MCP server would attach to both without the hand-mapping the hooks require; what it does not settle is whether the port authors want the component or how each harness's tool permissions compare. Read from vendor docs, untested → port-harness-mcp-support.md
- **The OpenCode port assessment pack, verbatim, and what generalises to the next port (2026-08-30).** The five files sent to the throughliner-opencode maker — cover note, 13-finding QUEUE, five-pillar BOOTSTRAP checklist, [upstream-catch-up] cycle, adapted self-hosting block — kept as the claims record for the register line and as the first data point of the self-hosting-onboarding research ([self-hosting-onboarding-research]); opens with the reusable method: lenses, findings-as-a-queue delivery, bootstrap gate, per-port cycle, hand-delivered seed → opencode-port-assessment-pack-2026-08-29.md
- **The release chain cannot deliver a validated build (2026-08-27).** Four findings: validation always runs on a build the tree has already moved past, so the ritual packages the least-tested state; the test-rezips entry lifecycle shipped the same day attaches a zip with the same flaw; `content_stamp()` hashes raw bytes under `core.autocrlf=true` with no `.gitattributes`, so a commit can never be compared to an installed build (measured: identical code, different stamps, every diff line-endings only); and the weekly cycle's stable-label selector has no label-to-commit or commit-to-release mechanism behind it. Includes the day's facts (d31b553 = the validated -test2 build) and four undecided fix directions → release-validation-chain-broken.md

**No length cap of any kind.** A line too short to support the open-or-skip
decision fails at any length; a line that reproduces its file fails too.

**Filing a finding writes its index line in the same move.** A file added without
a line is invisible to every later session, which is the blindness this index
exists to end.

- **Discord-verified GitHub identity for multi-user sessions — the handshake exists natively** — Discord Connections + Linked Roles let a server stamp a member with a role attesting to a real GitHub login, no custom bot; the method's side only needs "identity is what the authenticated channel supplies", and GitHub-side credit is commit co-author trailers, no branches. Limit: what metadata a bot can read (vs gate on) is unverified → `discord-github-linked-roles.md`

**A `Superseded by:` line at the top of a file is noted here too**, so a session
scanning the index sees the correction before it opens the file.

---

- **Marketplace name collision — `marketplace add` silently overwrites, verified 2026-08-27** — two sources declaring the same marketplace name means the second add silently replaces the first (open bug anthropics/claude-code#44042); this repo's committed name `flintcraft` matches the local rezip-source marketplace, so the beta install command on the rezip machine would silently repoint installs to GitHub. Behind the never-add-on-this-machine CLAUDE.md rule; re-check if the bug ships a fix → `marketplace-name-collision.md`
- **What LOG/index.md costs to read — measured 2026-08-24** — 990 index lines, ~354,000 characters, roughly 88,000 tokens if read in full as the retrieve rule then instructed; 1,007 records in LOG/ growing at ten-plus lines per working day; median index line 41 words. Converted [log-retrieve-search-leg] into a build (search-first retrieval, per-month index split); the derivation any future index bound cites, with re-measure commands at the end → `log-index-read-toll.md`
- **How Throughliner gets listed in the Claude marketplace — two routes, verified 2026-08-22** — the official marketplace is the only one browsable in-app by default and is curated at Anthropic's discretion with no self-serve path; the community marketplace takes submissions via the clau.de/plugin-directory-submission form (automated security scan + human approval, listings pinned to a commit SHA), but users must add it manually. Marketplace names are immutable once published; version consistency between plugin.json, changelog and tags is the commonest rejection cause. Read before planning any marketplace submission. Also carries the beta-channel install path (verified 2026-08-22): marketplace-adds pin to a branch/tag via `#ref`, so a moving `beta` branch serves testers through the README's existing ask-Claude install shape; and the update-cadence finding (2026-08-22): listing updates re-enter review with no documented turnaround, so the listing trails the weekly stable channel on a slower, submit-and-wait rhythm → `claude-marketplace-listing-paths.md`
- **Can a plugin ship an output style that applies itself? — yes, verified against the official docs 2026-08-22** — plugins ship styles in `output-styles/`; `force-for-plugin: true` applies the style automatically whenever the plugin is enabled, overriding the user's own `outputStyle` (first plugin loaded wins on a clash); `keep-coding-instructions: true` retains the built-in coding instructions; styles reach the main conversation only, take effect after /clear or a new session, and sit at system-prompt level above CLAUDE.md content — which makes evicting a style-carried rule from the docs below parent-axis dedup. Built-in Concise style exists from v2.1.237. Read before building [ship-brevity-output-style] or its dedup audit → `output-styles-plugin-capability.md`
- **Does a work item's rationale flow into the shipped docs? — tested against git, confirmed** — reasoning written into a queue item reaches the procedure doc the build edits, in recognisable and often near-verbatim form. Two cases from commit `de2f5fc`, read from both ends (the item text removed from QUEUE.md against the text added to `plan.md` in the same commit). The settling case is [tool-build-implies-an-audit], where the item directed nothing about what should ship and the doc still gained its clause order intact with the final sentence identical. Proportion measured in one hunk: ~8 lines of operative rule against ~17 of explanation. **Claims that the mechanism operates, not a rate** — no counterfactual sample was run, so items whose reasoning stayed put were not looked for. Read before treating rationale accumulation in the always-loaded corpus as an authoring-discipline problem, and before running the rationale audit or the restyle's per-paragraph lens, both of which remove text this puts back → `rationale-flows-from-items-into-shipped-docs.md`
- **How long this project's four written shapes have actually grown, measured rather than assumed** — a replay of QUEUE.md's whole git history plus every per-entry LOG file, reporting word count against date for captures, work items, LOG entries and LOG index lines. The headline: everything roughly doubled during August 2026. Planning entries went from a 323-word median in July to 895 in August (mean 1,010, max 3,658); build entries from 229 to 478; **index lines from a 40-word median to 76**, with the longest at 337 words. Captures nearly doubled at first filing, 176 to 336, and a work item currently in Processed has typically gained another 141 words between capture and being kept. Confirms the user's structural point: the per-entry split shrank each entry's scope and the entries grew anyway. Regenerate with `py plugin/throughliner/scripts/measure_written_shape_length.py . --history` (the script moved into the plugin package on 2026-08-20, and the history report moved behind `--history` when the default became the current-distribution report). **States no band and no threshold, deliberately** — this is the bloated corpus, so a range read off its middle would enshrine the bloat; the band is derived afterwards from specimens judged good. Coverage limit: the queue's pre-two-section history parses to nothing, so the record starts 2026-07-02. **Extended 2026-08-15 with the before/after test the length rule was waiting on:** August split at the rule's 2026-08-13 ship date shows capture length ROSE rather than fell — median 321 to 396, mean 368 to 461, n=152 after. The third prose remedy has a measured non-result, which is what licenses designing a non-prose one → `written-shape-length-growth.md`
- **Can a model write to a length target without measuring?** — the friend's claim is right and is the field's named root cause: LIFEBench finds models cannot recognise how many words they have generated. But two findings cut the other way and both favour a range here — the documented failure direction under a length instruction is **undershoot**, not overshoot (reproduced exactly by this project's Discord posts landing at ~75% of 2,000 chars every time), and short targets are followed reasonably while the sharp deterioration sits at thousands of words, which is a regime none of this project's shapes occupy. Exact compliance is achievable by three published methods and all three are "generate, measure, regulate", which Alex refused. Extended the same day with the unit question and the range's form: **words, not characters** — character counting runs near a 33% random baseline while word constraints are complied with markedly better, so a range here is stated in words and the Discord character limit works only because it is a ceiling to stay under rather than a target to hit. A **soft band plus a hard ceiling with a named action on breach** is the documented hard-versus-soft constraint pattern, and the action-on-breach is what answers this project's own repealed cap, since advising is not refusing. Also carries the literature's derivation method for the figure — set limits from measured good generations rather than choosing a number. **Corrects its own first reading:** Anthropic's exemplar guidance and this project's local proof of it are about SHAPE, not length, so they do not defeat a numeric range → `llm-length-instruction-following.md`
- **Where the four skill docs restate rules the always-loaded doc already carries** — a 2026-08-14 audit run by an outside Claude session with no context on this project, given only the five docs and asked to find unnecessary repetition. 17 findings plus 3 inverse findings (rules repeated across skills but absent from the always-loaded doc, so canonical nowhere). The heaviest: the red-flag clearing lifecycle written out five times, the `[user]` capability check three times inside one sub-step of `plan.md`, provenance three times, the flavor-marker table three times. Also flags a specification conflict in `setup.md` about when the always-loaded rules are in force. Line numbers are as of that date and its four sub-docs (`next-build.md`, `next-audit.md`, `done-*.md`) were never supplied, so its coverage is partial by its own account → `method-doc-redundancy-audit.md`
- **What a FAQ is for, and the technical-writing case against having one** — the mainstream position (GDS, A List Apart, passo.uno, over a decade, independently) is against having a FAQ at all, because the format produces the failure: FAQs come from needing somewhere visible to put pressing questions rather than a deliberate choice, the answer sits away from the material it belongs to, duplicated content falls out of sync, and the page becomes a dumping ground. Records the one question no source answered — what to do when the "context" the answer should live in is a procedure doc the reader never opens — which is why this project departed from the research and kept its FAQ → `faq-as-a-document-type.md`
- **Steering response length and one-at-a-time delivery — what actually works** — Anthropic's current guides on stopping bundling and on delivering detail less painfully, run because Alex is fatigued by long responses. Narrows `opus-4-8-verbosity-steering.md` (whose "quantified targets beat adjectives" advice is 4.8-era) and extends `opus-5-instruction-compliance.md` → `response-length-and-bundling-steering.md`
- **skill-nonspecific-rules.md — contradictions and duplicates** — a full audit of the always-loaded file at 1,037 lines, by a separate Claude instance, split into contradictions (two rules giving opposing instructions), duplicates (the same rule stated twice, so it can drift), and admission-test failures (rules that do not fire in all four skills, which the file's own opening test forbids). Line numbers are as supplied and have drifted → `skill-nonspecific-rules-conflicts-audit.md`
- **The index-line proportional cap, measured against the whole LOG corpus** — the measurement that repealed the 20% cap: it proved monotonic in *entry* length rather than line length, so it never fired on the long entries it was aimed at and fired 117 times on work nobody thinks is wrong. Kept as a durable file because a later session must be able to re-read it before restoring or re-deriving a figure → `index-line-length-distribution.md`
- **The 150–200 instruction ceiling, re-validated against the 5-series** — the figure is roughly an order of magnitude too tight for the models this method targets; the benchmark it came from was re-run a year later and frontier models had improved about tenfold. Supersedes §1 of `instruction-file-bloat-and-subtraction.md` on the number only → `instruction-ceiling-revalidated-for-5-series.md`
- **What a SPEC should contain, how long it should run, and whether ours conforms to a standard** — there is no length standard and every source says so explicitly, so "is 6,000 words too long?" cannot be answered by appeal to one. Observes that large stretches of this project's SPEC describe hook internals, marker-file fields and version history, which is implementation description rather than product truth. The derivation behind the shipped SPEC-admission rule → `spec-document-standards.md`
- **Rationale growth, measured** — a script walked every commit touching QUEUE.md and measured how rationale prose grows. Consolidated from five queue items that were deleted once their content landed here, because none of them was work: ask what changes inside which files and the answer was nothing → `rationale-growth-measurement.md`
- **Worktree isolation, desktop parallel sessions, and what this machine actually does** — the docs say every new desktop session gets its own worktree at `<project-root>/.claude/worktrees/`, and no worktree has ever appeared on this machine; the contradiction was reported to Anthropic as `anthropics/claude-code#85560`. Extended 2026-08-14 with the cloud-clone isolation case and its measured environment discriminator, a recorded specimen of a deliberate long-lived worktree, and the refutation of the reused-session hypothesis — a genuinely new desktop session got no worktree either, so the contradiction stands with its leading explanation removed → `worktree-isolation-and-desktop-sessions.md`
- **Can a hook force the behaviour rules to be read?** — `SessionStart` supports context only and cannot block or deny, so nothing can force the read *at session start*; any forcing mechanism has to fire later. Run because `session_start.py`'s behaviour-rules directive is an instruction with no enforcement behind it. **Enforcement refused 2026-08-19** — the findings stand, but a gate proves the file was opened and every recorded failure here is read-and-not-followed → `hook-enforced-doc-reading.md`
- **How legislative prose is actually shaped: sentence syntax and the test for subordination** — the Coode three-part legislative sentence, and the test this project's rule gate now uses to decide whether a new rule is genuinely a subordinate unit of an existing one rather than a freestanding rule wearing a bullet → `legislative-prose-syntax.md`
- **How law puts legislation on a review cycle — and where those cycles fail** — sunset clauses, post-legislative scrutiny, and consolidation; researched for the rule-lifecycle design, and the source of the "default state does the work" principle behind `retired-terms.md`. Also records why a calendar-based cycle does not transfer here → `legislative-review-cycles.md`
- **Prior art: the always-loaded universal behaviour doc** — the pattern is not novel, it is the ecosystem default (`CLAUDE.md` / `AGENTS.md` are exactly this artifact), and it fails the same way for everyone: monotonic growth with no eviction. Answers whether the constant audit-and-eviction maintenance this project does is inherent → `universal-behaviour-doc-prior-art.md`
- **Writing long agent documentation: avoiding additive noise, and how to subtract** — **Superseded in part** by `instruction-ceiling-revalidated-for-5-series.md`: §1's 150–200 figure fell, and anything scoped against that number needs its premise re-read. The rest stands and is the more useful half — the relevance argument, near-identical rules as optimal distractors for one another, and the subtraction techniques. Researched after the emergency revert caused by the always-loaded doc growing from 6,162 to 21,445 words → `instruction-file-bloat-and-subtraction.md`
- **Borrowing from legal drafting: packing rules tightly, and amending instead of adding** — the amendment-versus-freestanding distinction that the rule gate's admission test is built on, plus consolidation and recasting. The user's framing: the method writes a rule and then writes its exceptions as massive explanations, when they should be subclauses → `legal-drafting-for-tight-rules.md`
- **Pronoun use, working memory, and how much to name things explicitly** — the repeated-name penalty is a robust decades-old result: removing pronouns makes comprehension *worse*, not better. Run after a live failure where Claude switched what "it" pointed at mid-paragraph and then repaired it by stripping pronouns out, which the user reported as condescending → `pronoun-use-and-working-memory.md`
- **Learning mode — what the evidence says about learners in AI-assisted coding** — the audience is real and large (76% believe learning to code is still worth it; 45% considering it), and the surrounding content economy splits into a "understand your code or it explodes" camp and a vibe-coding camp. Researched for the learning-mode fork at /setup → `learning-mode-learner-needs.md`
- **Claude Code hook events — the current registrable set, and what `tool_name` carries** — the documented event list and payload fields, filed because three queue items rested on claims about which hook events exist. Records a non-answer honestly: the docs never name the subagent-spawning tool for a `PreToolUse` payload, so whether "Task" still arrives is undocumented → `claude-code-hook-events.md`
- **Codex port post-mortem, with Rygel's OpenCode port as a comparison** — the shelved Codex port's timeline from its own LOG, the founding "one method, separate products" contract, and what went wrong; audited against the current method on main. Read this rather than the port folder when asking why the port was shelved → `codex-port-postmortem.md`
- **scopelock v1 — an independent implementation of the same idea, by someone else** — a zero-ceremony scope lock for AI coding agents, enforcing from an explicit `SCOPE.md`, with scope *inference* deliberately excluded from v1 because inferred scope produces false denials and false denials train workarounds. **This is a folder of three files, not a single document** (`README.md`, `AGENTS.scopelock.md`, `scopelock.ts`) → `scopelock-independent-implementation/`
- **Scope-lock audit — the shipped plugin package** — an 11,000-word function-by-function audit of everything under the plugin package against the property that the agent works only inside boundaries the human defined. Names the four layers that produce scope lock: hook code that denies, procedure clauses that instruct, skill frontmatter that gates entry, and template text that seeds the same rules into the user's project → `scope-lock-audit.md`
- **Does instruction *format* affect rule adherence? — fresh literature check** — nobody has tested the question this project actually has: prose versus pseudocode for *standing behavioural rules*, as opposed to task instructions. Run to re-ground a register decision that had rested on one section read once → `pseudocode-vs-prose-for-rule-adherence.md`
- **SessionStart hook output schema — why our injection stopped reaching sessions** — Claude Code requires the hook to nest `additionalContext` under `hookSpecificOutput`; ours emitted it at the top level, so the hook exited 0, printed valid JSON, and had its output silently discarded. Filed because the finding cancelled a planned Claude Code bug report → `session-start-hook-output-schema.md`
- **Opus 5 instruction-following — and what it means for the multi-model fork** — Opus 5 is fussy in the *opposite* direction to 4.8: it over-does rather than under-does, so the official guidance is subtraction. This is what closed the one-docset-per-model fork, since Fable 5 wants the same thing → `opus-5-instruction-compliance.md`
- **Pseudocode / structured formats for skill docs** — the Skill-as-Pseudocode paper (arXiv 2605.27955) names this project's exact problem, that free-form prose forces the agent to re-derive structure on every read. Flags in its own text that the evidence base measures something other than what this project cares about, which is what `pseudocode-vs-prose-for-rule-adherence.md` later followed up → `pseudocode-format-for-skill-docs.md`
- **Plugin/marketplace rename migration — how a rename reaches existing consumers** — GitHub 301-redirects a renamed repo so existing marketplace registrations keep resolving, with the caveat that a new repo must never reuse the old name. The feasibility answer that let the Throughliner rename happen as a big bang → `plugin-marketplace-rename-migration.md`
- **status-line context-% reader — feasibility** — confirmed: Claude Code pipes JSON to a custom status-line command on every update, carrying a `context_window` object with `used_percentage` and `remaining_percentage`. So a script *can* surface context-% to a file, which is the one route by which anything in this method could ever know how full the window is → `statusline-context-reader-feasibility.md`
- **Name availability — "Throughline" vs "Throughliner"** — "Throughline" is crowded and one competitor is itself software (a macOS/Windows story-writing app), plus a mental-health platform. The search behind choosing "Throughliner" → `throughliner-name-availability.md`
- **Deep-interview patterns for /setup** — modern AI interview agents are adaptive rather than branching: they read each answer and generate the next question from what is still uncovered. This is what turned /setup's five questions into an example bank rather than a fixed script → `setup-interview-depth-patterns.md`
- **Can Claude read its own remaining context window mid-run?** — no. The claimed `<budget:token_budget>` and `<system_warning>` injection tags are not real; the actual `budget_tokens` parameter governs extended thinking. This is why nothing in the method may claim to sense the context window filling, and why the user's report is the only trigger → `context-window-visibility-to-model.md`
- **Can the SessionStart hook detect which model is running?** — yes, with a caveat that shapes any design resting on it: `SessionStart` is the one hook that can carry a `model` field, but the field is documented as not guaranteed to be present → `sessionstart-hook-model-detection.md`
- **Remote-control limitations and levers** — two pain points from working chiefly by phone: Claude regenerating self-authored text into chat for approval (inherent to LLMs, not a Claude Code oversight), and what view-in-doc cannot do when viewing a file means navigating Drive. Relevant to any question about how the method behaves away from the desktop → `remote-control-limitations.md`
- **Claude "copying" vs generating text — and what the view-in-doc fix really rests on** — there is no clipboard channel; every token is regenerated, so "Claude can't copy text" was never the real constraint. Short-to-moderate blocks reproduce reliably; the real constraints are two, and neither is copying → `claude-copy-vs-generate-text.md`
- **Fable 5 compatibility with the project's Opus 4.8-specific authoring techniques** — Fable 5's instruction following is strong enough that most of the 4.8 compensation machinery is unnecessary, and the official guide warns it can now *hurt*: skills built for prior models are often too prescriptive and degrade output quality → `fable-5-instruction-compatibility.md`
- **Claude Code plugin install/update mechanism** — still a cached snapshot, not live: on install the plugin is copied into `~/.claude/plugins/cache/...` and that copy runs, so republishing changes nothing until reinstall plus restart. The version is keyed on the source's git commit SHA. This is the fact the whole host-versus-target distinction rests on → `claude-code-plugin-cache-mechanism.md`
- **Installing a custom/local plugin in Claude Code (desktop app)** — the desktop app's in-app local plugin install has been **removed**; the supported path is the `claude` CLI (`plugin marketplace add` + `plugin install`) or a GitHub-hosted marketplace. This is why the rezip ritual runs through the CLI → `claude-code-plugin-install-paths.md`
- **Dependency tracing at plan time — prior art** — "surface dependency problems at planning time, not during execution" is an established, validated principle, and no static dependency check can ever be complete (stated outright by Bazel). The honest limit behind this method's plan-time tracing → `dependency-tracing-at-plan-time.md`
- **When a subagent is genuinely needed — context-overflow vs token-cost** — a subagent's real benefit is a separate context window: heavy raw material goes in, a short dense summary comes back. A concrete list of the rare cases that genuinely need one, written so the rule does not have to be guessed at and over-asked → `subagent-genuine-need-cases.md`
- **Autonomous build-loop design — prior art** — the field's settled answers on stopping conditions and human-in-the-loop gates. Its most universal safeguard is explicit hard stops (iteration ceiling, no-progress detection, budget ceiling); this method deliberately kept only no-progress detection, so read this before re-proposing the others → `autonomous-build-loop-design.md`
- **Spec-driven development: does inline spec editing comply?** — yes, and it aligns better with mainstream SDD than a separate spec-edit batch did. The finding that retired the spec-edit batch type in favour of in-session /plan edits plus the build's scope-lock ask → `spec-driven-development-edit-workflow.md`
- **Subagent ask-gate hook — feasibility** — yes: `PreToolUse` hooks return a `permissionDecision` of allow / deny / ask / defer, and `"ask"` surfaces a prompt the user answers. The "guarantee the ask, keep the choice" mechanism, filed after a silent deep-research fan-out blew through usage → `subagent-ask-gate-hook.md`
- **AI memory/context trajectory — what the ground under "heavy docs in context" is doing** — the bet is getting safer on cost and memory; the standing risks are quality-at-scale and tier-gating. Covers three axes separately because they have different answers → `context-window-cost-memory-trajectory.md`
- **Hook context-injection vs a companion CLI** — the mechanism for collapsing a multi-read skill startup into one pre-assembled bundle exists, and it is a **hook, not a companion CLI**. Read this before any proposal to ship a consumer-installed helper tool → `hook-context-injection-vs-companion-cli.md`
- **How Claude Code plugins collect user feedback** — Claude Code's built-in `/bug` does **not** cover third-party plugins, so a consumer hitting a method problem has nowhere right to put it. The research behind the flintcraft.tech/report channel → `plugin-feedback-channel.md`
- **Spec structure for multi-product / multi-area projects** — the established pattern is two-layer, and the finding that informed keeping one SPEC.md restructured project-shaped rather than splitting into per-product specs → `spec-structure-multi-product.md`
- **Claude Code plugin enable/disable — when does it take effect?** — not in the running session: hooks, MCP config, agents and output styles need `/reload-plugins` or a restart, and in cloud sessions `/reload-plugins` is disabled entirely. Notes an open bug where disabled plugins still inject context via SessionStart → `plugin-enable-disable-session-lifecycle.md`
- **Index + detail document architecture for AI agents** — the "consolidated index + on-demand detail" shape is an established named pattern, progressive disclosure, and is the mainstream agent-context approach rather than a risky invention. The prior art behind LOG/index.md and this file → `index-detail-doc-architecture-for-agents.md`
- **Steering Opus 4.8 toward concise, user-readable output** — **narrowed by** `response-length-and-bundling-steering.md`, whose Opus 5 guidance governs the current model; its "quantified targets beat adjectives" advice is 4.8-era. Corrects the older assumption that the system prompt's thoroughness directives outrank any brevity instruction. History, not a live target → `opus-4-8-verbosity-steering.md`
- **Claude Code plugin install mechanisms (CLI / desktop / agent)** — the desktop app is Claude Code with an integrated terminal, and CLI/desktop have plugin parity, so a desktop user does have terminal access and the agent can run shell commands. The basis for a "let Claude Code install it for you" README pathway → `claude-code-plugin-install-mechanisms.md`
- **Model instruction compliance across Opus 4.6 / 4.7 / 4.8** — Anthropic's own postmortem confirming three harness-level changes (reasoning-effort downgrade, thinking-cache bug, a verbosity system prompt) caused the 4.6 quality drop, rather than model nerfing. History: 4.8 is no longer a supported target → `model-instruction-compliance.md`
- **Auto-memory staleness — is Claude Code's memory system harming us?** — yes, and it is a recognised failure mode: auto-memory accumulates but does not curate, so snapshots of project state rot fast while behavioural and preference memories stay durable. The distinction behind this project's memory-boundaries rule. **Re-verified 2026-08-15 and partly corrected:** the staleness finding stands, but AutoDream is now live — auto-triggering after roughly 24 hours plus five sessions, so the file's "we don't have it running, cleanup is manual" no longer holds; and "Obsidian memory systems" is a whole category of independent projects rather than one thing, which any comparison must say. **Re-verified again 2026-08-23:** the Obsidian half is reinforced, but the `/dream` *command* is still behind the gradual rollout — consolidation is reachable by every user only by asking in plain words, so no comparison may claim everyone has the command → `auto-memory-staleness.md`
- **Batch sizing research** — the constraint on a batch is attention budget, not token count: adherence to each instruction drops as simultaneous requirements pile up. Recommends 60–80% context utilisation rather than maxing out. Predates the current run model, so read the finding rather than the sizing advice → `batch-sizing-research.md`
- **Context window data in plugin hooks** — the desktop app computes a full context breakdown and does **not** expose it to hooks or plugins; lists what each hook event's JSON actually carries. Paired with `statusline-context-reader-feasibility.md`, which found the one channel that does → `context-window-hook-access.md`
- **Live preview of QUEUE.md (and other project docs)** — the desktop Files panel renders a file when clicked but does not live-update as it changes on disk; a known unfixed limitation with an open feature request, not something a workaround inside the app reaches → `live-queue-preview.md`
- **Plan Panel Integration Research** — how the desktop Plan panel works (EnterPlanMode → a markdown file in `~/.claude/plans/` → ExitPlanMode → Approve/Reject), and why this method is incompatible with it. Read before any proposal to render the queue through the Plan panel → `plan-panel-integration.md`
- **Suggestion-chip replies and what can evidence a user's authorship** — transcripts record who *sent* a message and never who *composed* it: typed replies and chip clicks are byte-for-byte identical (`origin: {kind: "human"}`, `promptSource: "sdk"`), so the primary evidence for provenance cannot answer the provenance question. Describes the loop that launders Claude's own proposal into a user credit, and the one signal that survives — a reply wholly contained in Claude's preceding message cannot evidence authorship. Read before any proposal to verify credits from the transcript, or to notify users about chip use → `chip-replies-and-provenance-evidence.md`
- **Line-anchored file links** — whether a `path.md:42` reference ever reaches an application: external evidence that Claude Code drops the line number **silently, before any app is launched** (`anthropics/claude-code` issue #83475), and that where line numbers do survive they appear to reach a *configured editor* rather than the file type's default handler, with nothing confirming a default-handler launch on Windows ever receives one. Confirms the shipped rule — plain file link, line named in the prose — so read this before any proposal to promise a line anchor → `line-anchored-file-links.md`
- **Tersifying QUEUE.md — the procedure, from two real passes** — nine cut/keep rules, the list preserved unconditionally, and seven recorded **declines** which the author calls the more important half. Two failures: a silent duplication of 15 items caught only by a slug-uniqueness check, and a probable **upgrade of a paraphrase into a quotation claim** — one instance of which was found still live in this queue on 2026-08-18 and repaired. Yields were 8% and 3%, and its own conclusion is that the queue is not verbose, so a wording pass is not the lever. Read in full before running any compression over the queue; do not paraphrase it → `tersifying-the-queue.md`
- **Skill content lifecycle — a skill invocation never ends another skill** — Claude Code's documentation states that an invoked skill's content "enters the conversation as a single message and stays there for the rest of the session", so there is no running skill, nothing to exit, and nothing to return to: after `/rescan` runs inside a `/plan`, `plan.md` is still present and still governing. The documentation names the exact symptom of a skill seeming to stop working and attributes it to the model's attention rather than the harness. Re-invoking an unchanged skill costs a one-line note, not a second copy. **The one genuine limit is auto-compaction**, which re-attaches only the newest skills within a 25,000-token budget. Read before any proposal to build a return-to-the-previous-skill mechanism, or any design assuming a skill is a mode with an end → `skill-content-lifecycle.md`
- **Why the close is heavy — measured** — a planning close read **10,297 words of procedure** (`done.md` 7,443 + `done-plan.md` 2,854) to write **3,603 words of record**, about 2.9:1 against the output, so the record is not the expensive half. `done.md` is a fixed toll on every close in every project; two of its fourteen sections are 45% of it, and a planning close reads and discards the completion check, the build/audit body fields, the audit approval line and the red-flag lifecycle. Same shape the build view was built for, one layer out. Names the lever the two queued restyle items do NOT cover: distribution, not wording — flavour-specific material sitting in the file every flavour reads. Read before proposing shorter session records as a cost fix → `why-the-close-is-heavy.md`
- **Discord: thread permissions, and whether forum guidelines are visible** — **"Send Messages in Threads" is independent of "Send Messages"** (Discord's words: the send permission "will no longer have any direct effect on Threads behavior"), so a locked channel can still host one open thread; leaving "Create Public Threads" off makes that thread the only conversation, which is how `#announcements` now takes feedback. Second finding is a **contradiction rather than a fact**: forum post guidelines are documented as appearing when a member clicks New Post, and the server owner observed on 2026-08-29 that they do not — two untested explanations recorded, neither confirmed. Read before planning anything on documented forum-guideline visibility, or before re-deriving how a locked channel can accept replies → `discord-permissions-and-forum-guidelines.md`
- **Run-cost measurements 2026-08-28 — navigation share, scoping vs transport, re-reads** — the MCP debate's evidence base: navigation is 22% of tool calls in both a build and a planning run atop a ~⅓MB fixed read cost no transport touches; scoping won a tenth with a script flag; no transport comparison was run and the plugin-MCP mechanics stay explicitly unverified; builds re-read 20 times where planning re-read 4. Carries a validity assessment; re-measure after the workshop restructure → `run-cost-measurements-2026-08-28.md`
- **Risk classes beyond security — what the record's misses actually were** — six instances classified (one scrubbed, from another project of the user's): the screen's three types are fine, but the costliest recorded class has no machinery at all — **unverified premise under sustained effort**, whose signature is cost accruing silently while work continues, invisible to write/send/commit checkpoints. Concludes a sibling screen is the question, decides no rule; any widening files as a gated capture citing this → `risk-classes-beyond-security.md`
- **Eliciting proactive research offers — why the noticing fails and what fires** — outside literature and this project's record agree independently: admit-uncertainty prompts help only where the model feels unsure, premise-driven errors bypass uncertainty entirely, and site-bound mechanical checks beat free-floating duties (five recorded siteless failures; the offer rule loaded and silent through the costly instance). Three candidate sites ranked, "rests on" line at the decision step strongest; each files as its own gated capture → `eliciting-proactive-research-offers.md`
- **Determinism lesson — the example bank across all four quadrants** — the quadrant map for [determinism-lesson-video]: surprisingly deterministic magic (OCR, barcode reading, EXIF, diffing — with the nuance that ML inside an engine does not make its output probabilistic), randomness-on-purpose without AI (shuffle, sampling, key generation, Monte Carlo), the bad-practice money cell (AI counting words, sorting, reformatting), and the bridge that is the lesson's best good-practice example: AI builds the deterministic tool once, then the tool runs free forever. From Claude's knowledge, not a web search; two claims flagged for a re-verify at drafting → `determinism-lesson-examples.md`
- **Recorded states in the method — computable or decision** — the inventory grounding the MCP umbrella's purpose hypothesis: every true/false-shaped fact the docs store, in five groups. Five states are already computed live and stored nowhere (the model); five are computable but stored (the version file, two run markers, the cache markers, the stop-claim marker); twelve are decisions no tool can compute, two of them explicitly uncomputable on the record (the rule-gate disposition, the format epoch); and four are MIXED — decision stored, resolution computed — of which `Blocked by:` and `Not before:` already do it and the build tick's confirmed field and the walk-through `done` do not. Two findings: the format epoch cannot be made computable and the refusal is recorded, and the register's "approved, not yet posted" is the one stored boolean the world can now answer. Carries a frame assessment; expect staleness within a few sessions → `recorded-states-inventory.md`
- **Scripted turns in the shipped docs — where defined, what tag, whether any content rule governs it** — every point the method requires Claude to say something with a particular shape, per doc, with its response-shape tag and whether anything states what the turn must CONTAIN. Tagged turns extracted mechanically (exhaustive); untagged turns found by reading (a floor, and the asymmetry is finding one — an untagged turn is invisible to any sweep, exactly as a prose-stated rule is invisible to `rule_signals.py`). Corrects the raising item's premise: `/rescan`'s turns are among the best governed in the corpus, while `done-build.md` and `done-audit.md` are the densest ungoverned region — 25 tags between them and essentially no content rules. Also: several `[PROMPT]` turns state only what NOT to say, and the one-line capture report has no subject-naming requirement where the planning summary turn does. Read before designing content rules for any turn, or before assuming a turn is unspecified → `scripted-turns-inventory.md`
- **Discord forum posts and notifications — what an admin can and cannot switch off** — there is **no per-channel notification default a server owner can set**: the only admin lever is the server-wide default (All Messages / Only @mentions), which applies solely to members who never customised theirs, and per-channel Notification Overrides are a per-member setting that cannot be imposed. The evidence for absence is three open Discord feature requests asking for it, one naming the forum case exactly. So nothing can be switched off for a specific forum; what decides whether a post pings someone is their own level plus whether the post mentions them, and our posts mention nobody. Unresolved: whether explicitly *following* a forum notifies regardless of the server default — the official Forum Channels FAQ returned 403 and was not read. Carries a frame assessment → `discord-forum-post-notifications.md`
- **Discord message limits — bots are capped at 2,000 characters and no paid plan lifts it** — full Nitro raises a human account's messages to 4,000, Nitro Basic raises nothing, and neither reaches a bot: the API content field is 2,000 on every send-or-edit endpoint whatever the owner's plan. Prices as of 2026-09-02 recorded. Settles that room for the rezip-entry backfill must come from structure (a threaded reply), not from paying. Carries a frame assessment → `discord-message-limits-and-nitro.md`
- **How Claude Code runs hook commands on Windows — in flux** — hooks have run through Git Bash and a PowerShell backend is arriving, so a `py`-then-`python` fallback line depends on shell syntax that differs between them; two reported silent-failure causes on Windows (`.sh` opening in an editor, CRLF). Settles that the hook command lines are not changed on a search alone: the shell is read at build time and the line is tested on a second Windows machine first. Carries a frame assessment → `claude-code-hook-shell-on-windows.md`
- **Desktop app plugin install routes — the CLI commands do not exist there; the app has Add marketplace / Upload plugin / Create with Claude** — `/plugin` and the two `claude plugin` commands are CLI-only, while the desktop app adds a marketplace from Settings → Customise → Plugins → Add (seen 2026-09-02). What the field accepts is unread and is [desktop-add-marketplace-verified]'s second-machine read. Settles that the install guide's plugin branch rested on a false premise and falsifies CLAUDE.md's "in-app upload is gone". **Updated 2026-09-10:** the docs' desktop page now documents the + button next to the prompt box → Plugins → Add plugin, and no in-app marketplace add — the 2026-09-02 menu path is not in any documentation. Carries a frame assessment → `desktop-app-plugin-install-routes.md`
- **Docset length across the law-prose rewrite — the restyle shortened nothing, the 2026-08-21 decision-history cut took 11%, and the docset has grown a third since** — word counts of the rules file and the whole procedure docset at nine commits from 2026-08-16 to 2026-09-04, read from git. Settles what the law-prose article may claim about length: the cut belongs to the reasons-out-of-the-rule convention, not to the restyle, and growth comes from admission. Carries a frame assessment → `docset-length-across-the-law-prose-rewrite.md`
- **Does a session see its own earlier thinking — yes on Opus 4.5 and every 4.6-and-higher model, no on Sonnet 4.5, Haiku 4.5 and earlier** — Anthropic's extended-thinking docs, read 2026-09-11: prior turns' thinking blocks are kept in context and billed as input on the current models and were stripped on the older ones, so how-to topic 6's claim that /rescan reads back Claude's own thinking holds on the method's target models — `prior-turn-thinking-in-context.md`
- **How other fields audit a layered rule set for restatement — legislative drafting, policy manuals, technical documentation** — all three separate one owning site per rule from every other mention (a pointer), audit by building the owner map first, and treat a cross-reference that no longer says what it claims as its own error class; prose restating a block or a specimen is legislative drafting's "purpose statement" defect; none runs revision weekly. Read 2026-09-05 for the maintenance sweep's restatement lens. Carries a frame assessment → `rule-set-maintenance-audits-other-fields.md`
